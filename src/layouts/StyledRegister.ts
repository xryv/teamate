import styled from 'styled-components';
import tw from 'twin.macro';

export const StyledRegister = styled.div`
    ${tw`relative flex flex-col items-center justify-center w-full h-fit bg-transparent my-36`}
    h1{
        ${tw`text-5xl border-b  px-8 py-4  text-orangePV-1000 backdrop-blur-xl m-10 bg-transparent outline-1 `}
    }

`;
